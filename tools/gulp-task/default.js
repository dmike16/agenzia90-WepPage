/**
 * Default gulp task
 */
const gutil = require('gulp-util');

exports.default = (gulp)=>()=>{
  gutil.log(gutil.colors.blue('90 s r l s'));
  gutil.log(gutil.colors.green('Pratiche Auto'));
  gutil.log(gutil.colors.red('Tel 06 01905227'));
  gutil.log(gutil.colors.red('https://studio90srls.it'));
};
